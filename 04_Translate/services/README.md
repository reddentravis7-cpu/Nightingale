# services/

Deployable units go here — e.g. the Cisco IOS Quick Reference app
(Next.js + Supabase), built and committed by Code through `main` @
`21519eb`, currently living wherever that work was committed. Move or
symlink it into `services/cisco-qrg-app/` to match this repo's own
architecture rather than leaving it in a separate location.
