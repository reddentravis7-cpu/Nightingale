# NOT CANONICAL — read before touching this domain

Three independent, unreconciled drafts of this domain's Knowledge Block
layer exist as of 2026-07-28:

1. CTO's single consolidated block — `draft-only-knowledge-blocks.json`
   in this folder
2. Steward's 35-block manifest — includes 2 blocks using an unreviewed
   `Procedural` blockType (one safety-critical) and 5 using a non-schema
   `IndustryPracticeContext` type. Not included here — never received in
   full.
3. Research's separate 37-block file, different id scheme. Not included
   here — the paste was cut off mid-file and never completed.

CTO needs the complete, literal file content from both Steward and
Research before this can be merged into one canonical file and one id
scheme. Do not build new capability work on this domain until that
happens. See `PROJECT_STATUS.md` at the repo root for full detail.
